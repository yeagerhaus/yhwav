CarPlay A/B test (Metro blank screen)
=====================================

Applied changes:
1. ios/Rite/Rite.entitlements — com.apple.developer.carplay-audio removed (see XML comment).
2. ios/Rite/Info.plist — UIApplicationSceneManifest (multiple scenes + CarPlay delegate) removed (see XML comment).

How to test:
  Terminal 1: bun run start:sim
  Terminal 2: bunx expo run:ios
  (Or Xcode: pick a simulator, Product > Clean Build Folder, Run.)

Interpret:
  - Still blank / no Metro → likely not CarPlay entitlement/scene manifest; revert anyway before shipping.
  - JS loads → restore plist + entitlement from git, then add proper phone + CarPlay scene config per Expo / react-native-carplay.

Revert everything:
  git checkout -- ios/Rite/Rite.entitlements ios/Rite/Info.plist
  rm ios/CARPLAY_EXPERIMENT_REVERT.txt   # optional

Xcode “All Exceptions” breakpoint:
  Shared file: ios/Rite.xcodeproj/xcshareddata/xcdebugger/Breakpoints_v2.xcbkptlist
  If Xcode does not pick it up: Breakpoint navigator > + > Exception Breakpoint > All.
  Disable in the navigator if it stops on benign framework exceptions.
  Remove when done: delete that Breakpoints_v2.xcbkptlist or git checkout.
